<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Advance_Control.Views.Pages.UbicacionesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Advance_Control.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Advance_Control.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Advance_Control.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel Grid.Row="0" Margin="20,20,20,12" Spacing="8">
            <TextBlock FontSize="32" FontWeight="Bold" Text="Ubicaciones" />
            <InfoBar
                x:Name="StatusBar"
                IsClosable="False"
                IsOpen="False"
                Severity="Informational"
                Title="Red" />
        </StackPanel>

        <!--  Content  -->
        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <!--  Left: Mapa  -->
            <WebView2
                x:Name="MapWebView"
                Grid.Column="0"
                Margin="0,0,10,0" />

            <!--  Right: Lista y Formulario  -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Filtro por área  -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,8" Spacing="4">
                        <TextBlock FontSize="12" Text="Filtrar por área" />
                        <ComboBox
                            HorizontalAlignment="Stretch"
                            DisplayMemberPath="Nombre"
                            ItemsSource="{x:Bind ViewModel.Areas, Mode=OneWay}"
                            PlaceholderText="Todas las áreas"
                            SelectedItem="{x:Bind ViewModel.SelectedAreaFilter, Mode=TwoWay}" />
                    </StackPanel>

                    <!--  Encabezado lista  -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock FontSize="20" FontWeight="SemiBold" Text="Lista de Ubicaciones" />
                            <Button
                                x:Name="RefreshButton"
                                VerticalAlignment="Center"
                                Background="Transparent"
                                BorderThickness="0"
                                Click="RefreshButton_Click"
                                ToolTipService.ToolTip="Recargar">
                                <SymbolIcon Symbol="Refresh" />
                            </Button>
                        </StackPanel>
                        <Button x:Name="AddButton" Margin="0,8,0,0" Click="AddButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Add" />
                                <TextBlock Text="Agregar Ubicación" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!--  Lista  -->
                    <ListView
                        Grid.Row="2"
                        x:Name="UbicacionesList"
                        ItemsSource="{x:Bind ViewModel.UbicacionesFiltradas, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SelectedUbicacion, Mode=TwoWay}"
                        SelectionChanged="UbicacionesList_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:UbicacionDto">
                                <Grid Padding="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="SemiBold" Text="{x:Bind Nombre}" />
                                        <TextBlock
                                            FontSize="12"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="{x:Bind Descripcion}" />
                                        <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                            <Run Text="Lat: " />
                                            <Run Text="{x:Bind Latitud}" />
                                            <Run Text=", Lng: " />
                                            <Run Text="{x:Bind Longitud}" />
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                                        <Button Click="EditButton_Click" Tag="{x:Bind IdUbicacion}" ToolTipService.ToolTip="Editar">
                                            <SymbolIcon Symbol="Edit" />
                                        </Button>
                                        <Button Click="DeleteButton_Click" Tag="{x:Bind IdUbicacion}" ToolTipService.ToolTip="Eliminar">
                                            <SymbolIcon Symbol="Delete" />
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!--  Formulario  -->
                    <StackPanel
                        x:Name="LocationForm"
                        Grid.Row="3"
                        Margin="0,12,0,0"
                        Padding="12"
                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Spacing="8"
                        Visibility="Collapsed">

                        <TextBlock x:Name="FormTitle" FontSize="18" FontWeight="SemiBold" Text="Nueva Ubicación" />

                        <TextBox x:Name="NombreTextBox" Header="Nombre *" PlaceholderText="Nombre de la ubicación" />

                        <TextBox
                            x:Name="DescripcionTextBox"
                            Height="60"
                            AcceptsReturn="True"
                            Header="Descripción"
                            PlaceholderText="Descripción opcional"
                            TextWrapping="Wrap" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBox
                                x:Name="LatTextBox"
                                Grid.Column="0"
                                Header="Latitud *"
                                InputScope="Number"
                                PlaceholderText="19.4326" />
                            <TextBox
                                x:Name="LngTextBox"
                                Grid.Column="2"
                                Header="Longitud *"
                                InputScope="Number"
                                PlaceholderText="-99.1332" />
                        </Grid>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="SaveButton" Click="SaveButton_Click" Style="{StaticResource AccentButtonStyle}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <SymbolIcon Symbol="Save" />
                                    <TextBlock Text="Guardar" />
                                </StackPanel>
                            </Button>
                            <Button x:Name="CancelButton" Click="CancelButton_Click">Cancelar</Button>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
