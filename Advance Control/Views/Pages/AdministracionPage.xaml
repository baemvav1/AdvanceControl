<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="RootPage"
    x:Class="Advance_Control.Views.Pages.AdministracionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Advance_Control.Models"
    xmlns:adminItems="using:Advance_Control.Views.Items.Administracion"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,12" Spacing="4">
            <TextBlock
                FontSize="24"
                FontWeight="SemiBold"
                Text="Administración" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="Configuración central de usuarios, seguridad y permisos del sistema." />
        </StackPanel>

        <TabView
            x:Name="AdministracionTabView"
            Grid.Row="1"
            IsAddTabButtonVisible="False"
            SelectionChanged="AdministracionTabView_SelectionChanged"
            TabWidthMode="Equal">
            <TabViewItem Header="Usuarios" IsClosable="False">
                <Grid Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock
                                FontSize="18"
                                FontWeight="SemiBold"
                                Text="Usuarios" />
                            <TextBlock
                                TextWrapping="Wrap"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Listado unificado de login, estado, nivel y asignaciones operativas del usuario." />
                        </StackPanel>

                        <Button
                            x:Name="RecargarUsuariosButton"
                            Grid.Column="1"
                            Margin="0,0,8,0"
                            Click="RecargarUsuariosButton_Click"
                            Content="Recargar" />

                        <Button
                            x:Name="NuevoUsuarioButton"
                            Grid.Column="2"
                            Click="NuevoUsuarioButton_Click"
                            Content="Nuevo usuario"
                            Style="{StaticResource AccentButtonStyle}" />
                    </Grid>

                    <InfoBar
                        Grid.Row="1"
                        Margin="0,0,0,12"
                        IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
                        Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                        Severity="Error" />

                    <Grid Grid.Row="2">
                        <ProgressRing
                            Width="48"
                            Height="48"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{x:Bind ViewModel.Usuarios, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:UsuarioAdminDto">
                                        <adminItems:UsuarioAdminItemView
                                            DeleteRequested="UsuarioItemView_DeleteRequested"
                                            EditRequested="UsuarioItemView_EditRequested"
                                            Usuario="{x:Bind}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </TabViewItem>

            <TabViewItem x:Name="PermisosTabViewItem" Header="Permisos" IsClosable="False">
                <Grid Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock
                                FontSize="18"
                                FontWeight="SemiBold"
                                Text="Permisos" />
                            <TextBlock
                                TextWrapping="Wrap"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Sincroniza vistas y botones detectados, y define el nivel requerido por módulo o acción." />
                        </StackPanel>

                        <Button
                            x:Name="RecargarPermisosButton"
                            Grid.Column="1"
                            Margin="0,0,8,0"
                            Click="RecargarPermisosButton_Click"
                            Content="Recargar" />

                        <Button
                            x:Name="SincronizarPermisosButton"
                            Grid.Column="2"
                            Click="SincronizarPermisosButton_Click"
                            Content="Sincronizar"
                            Style="{StaticResource AccentButtonStyle}" />
                    </Grid>

                    <InfoBar
                        Grid.Row="1"
                        Margin="0,0,0,12"
                        IsOpen="{x:Bind ViewModel.HasPermisosError, Mode=OneWay}"
                        Message="{x:Bind ViewModel.PermisosErrorMessage, Mode=OneWay}"
                        Severity="Error" />

                    <Grid Grid.Row="2">
                        <ProgressRing
                            Width="48"
                            Height="48"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsActive="{x:Bind ViewModel.IsLoadingPermisos, Mode=OneWay}" />

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{x:Bind ViewModel.PermisosModulo, Mode=OneWay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:PermisoModuloDto">
                                        <adminItems:PermisoModuloItemView
                                            NivelAccionEditRequested="PermisoModuloItemView_NivelAccionEditRequested"
                                            NivelModuloEditRequested="PermisoModuloItemView_NivelModuloEditRequested"
                                            Permiso="{x:Bind}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </TabViewItem>
        </TabView>
    </Grid>
</Page>
