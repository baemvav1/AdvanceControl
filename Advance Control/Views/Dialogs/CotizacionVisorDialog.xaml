<ContentDialog
    x:Class="Advance_Control.Views.Dialogs.CotizacionVisorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Visor de cotización"
    PrimaryButtonText="Enviar por correo"
    SecondaryButtonText="Abrir con visor externo"
    CloseButtonText="Cerrar"
    DefaultButton="Primary">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1100</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">840</x:Double>
    </ContentDialog.Resources>

    <Grid MinWidth="900" MinHeight="580">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer
            x:Name="PdfScrollViewer"
            Grid.Row="0"
            MinHeight="540"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel
                x:Name="PdfPagesPanel"
                Padding="8"
                Spacing="12" />
        </ScrollViewer>

        <TextBlock
            x:Name="EstadoTextBlock"
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="No se pudo cargar la vista previa del PDF."
            TextAlignment="Center"
            TextWrapping="Wrap"
            Visibility="Collapsed" />

        <!-- Mensaje de carga -->
        <ProgressRing x:Name="CargandoRing"
                      Grid.Row="0"
                      IsActive="True"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Width="48" Height="48"/>

        <!-- Ruta del archivo -->
        <TextBlock x:Name="RutaTextBlock"
                   Grid.Row="1"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextTrimming="CharacterEllipsis"
                   Margin="0,6,0,0"/>
    </Grid>
</ContentDialog>
