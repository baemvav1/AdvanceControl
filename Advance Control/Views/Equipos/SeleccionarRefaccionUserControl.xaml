<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Advance_Control.Views.Equipos.SeleccionarRefaccionUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Advance_Control.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinWidth="500"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullableNumberToStringConverter x:Key="NullableNumberConverter" />
    </UserControl.Resources>

    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel
                x:Name="SearchPanel"
                Margin="0,0,0,0"
                Spacing="8">
                <TextBox
                    x:Name="MarcaTextBox"
                    PlaceholderText="Buscar por marca..."
                    TextChanged="SearchTextBox_TextChanged" />
                <TextBox
                    x:Name="SerieTextBox"
                    PlaceholderText="Buscar por serie..."
                    TextChanged="SearchTextBox_TextChanged" />
            </StackPanel>

            <StackPanel
                x:Name="SelectedRefaccionPanel"
                Margin="0,0,0,0"
                Visibility="Collapsed">
                <TextBlock
                    Margin="0,0,0,4"
                    FontWeight="SemiBold"
                    Text="Refacción seleccionada:" />
                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock
                                x:Name="SelectedMarcaTextBlock"
                                FontSize="14"
                                FontWeight="SemiBold" />
                            <TextBlock
                                x:Name="SelectedSerieTextBlock"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock
                                x:Name="SelectedCostoTextBlock"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Button
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Click="ShowListButton_Click"
                            Content="Cambiar" />
                    </Grid>
                </Border>
            </StackPanel>

            <!--  Loading indicator  -->
            <ProgressRing
                x:Name="LoadingRing"
                Width="50"
                Height="50"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsActive="False"
                Visibility="Collapsed" />


            <!--  Lista de refacciones  -->
            <ListView
                x:Name="RefaccionesListView"
                SelectionChanged="RefaccionesListView_SelectionChanged"
                SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--  ID Badge  -->
                            <Border
                                Grid.Row="0"
                                Grid.RowSpan="3"
                                Grid.Column="0"
                                Margin="0,0,12,0"
                                Padding="8,4"
                                VerticalAlignment="Center"
                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                CornerRadius="4">
                                <TextBlock
                                    FontSize="12"
                                    FontWeight="SemiBold"
                                    Foreground="White"
                                    Text="{Binding IdRefaccion}" />
                            </Border>

                            <!--  Marca  -->
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="1"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Text="{Binding Marca}"
                                TextTrimming="CharacterEllipsis" />

                            <!--  Serie  -->
                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="1"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{Binding Serie}"
                                TextTrimming="CharacterEllipsis" />

                            <!--  Costo  -->
                            <TextBlock
                                Grid.Row="2"
                                Grid.Column="1"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                <Run Text="Costo: $" />
                                <Run Text="{Binding Costo}" />
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!--  Selected Proveedor Panel  -->
            <StackPanel
                x:Name="SelectedProveedorPanel"
                Margin="0,12,0,0"
                Visibility="Collapsed">
                <TextBlock
                    Margin="0,0,0,4"
                    FontWeight="SemiBold"
                    Text="Proveedor seleccionado:" />
                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock
                                x:Name="SelectedProveedorIdTextBlock"
                                FontSize="14"
                                FontWeight="SemiBold" />
                            <TextBlock
                                x:Name="SelectedProveedorNombreTextBlock"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock
                                x:Name="SelectedProveedorCostoTextBlock"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Button
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Click="ShowProveedoresListButton_Click"
                            Content="Cambiar" />
                    </Grid>
                </Border>
            </StackPanel>

            <!--  Proveedores button and grid  -->
            <StackPanel
                x:Name="ProveedoresPanel"
                Margin="0,12,0,0"
                Visibility="Collapsed">
                <Button
                    x:Name="ProveedoresButton"
                    Margin="0,0,0,8"
                    HorizontalAlignment="Stretch"
                    Click="ProveedoresButton_Click"
                    Content="Proveedores" />
                <Border
                    x:Name="ProveedoresGrid"
                    MinHeight="100"
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Visibility="Collapsed">
                    <Grid>
                        <!--  Loading indicator for providers  -->
                        <ProgressRing
                            x:Name="ProveedoresLoadingRing"
                            Width="40"
                            Height="40"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsActive="False" />

                        <!--  Lista de proveedores  -->
                        <ListView
                            x:Name="ProveedoresListView"
                            SelectionChanged="ProveedoresListView_SelectionChanged"
                            SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!--  ID Badge  -->
                                        <Border
                                            Grid.Column="0"
                                            Margin="0,0,12,0"
                                            Padding="8,4"
                                            VerticalAlignment="Center"
                                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                                            CornerRadius="4">
                                            <TextBlock
                                                FontSize="12"
                                                FontWeight="SemiBold"
                                                Foreground="White"
                                                Text="{Binding IdProveedor, Converter={StaticResource NullableNumberConverter}, ConverterParameter=?}" />
                                        </Border>

                                        <!--  Nombre Comercial  -->
                                        <TextBlock
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Text="{Binding NombreComercial}"
                                            TextTrimming="CharacterEllipsis" />

                                        <!--  Costo  -->
                                        <TextBlock
                                            Grid.Column="2"
                                            VerticalAlignment="Center"
                                            FontSize="12"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                            <Run Text="$" />
                                            <Run Text="{Binding Costo, Converter={StaticResource NullableNumberConverter}, ConverterParameter=N/A}" />
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
