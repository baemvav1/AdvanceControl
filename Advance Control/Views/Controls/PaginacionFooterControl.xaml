<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Advance_Control.Views.Controls.PaginacionFooterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="0,8,0,0" ColumnSpacing="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock
            x:Name="RangoTextBlock"
            Grid.Column="0"
            VerticalAlignment="Center"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

        <StackPanel
            Grid.Column="1"
            Orientation="Horizontal"
            Spacing="4">
            <Button
                x:Name="PrimeraButton"
                Click="Primera_Click"
                Content="&#xE892;"
                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                ToolTipService.ToolTip="Primera página" />
            <Button
                x:Name="AnteriorButton"
                Click="Anterior_Click"
                Content="&#xE76B;"
                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                ToolTipService.ToolTip="Página anterior" />
            <TextBlock
                x:Name="PaginaTextBlock"
                MinWidth="90"
                Margin="4,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="12" />
            <Button
                x:Name="SiguienteButton"
                Click="Siguiente_Click"
                Content="&#xE76C;"
                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                ToolTipService.ToolTip="Página siguiente" />
            <Button
                x:Name="UltimaButton"
                Click="Ultima_Click"
                Content="&#xE893;"
                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                ToolTipService.ToolTip="Última página" />
        </StackPanel>
    </Grid>
</UserControl>
