<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Advance_Control.Views.Equipos.EditNotaRelacionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Advance_Control.Views.Equipos"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        Padding="20"
        Background="{ThemeResource LayerFillColorDefaultBrush}"
        CornerRadius="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="4,0,4,4"
            FontWeight="SemiBold"
            Text="{x:Bind ClienteNombre, Mode=OneWay}" />

        <TextBlock
            Grid.Row="1"
            Margin="4"
            FontWeight="SemiBold"
            Text="Nota:" />
        <TextBox
            x:Name="NotaTextBox"
            Grid.Row="2"
            Width="300"
            Height="100"
            Margin="4,4,4,12"
            AcceptsReturn="True"
            PlaceholderText="Ingrese la nota para la relación"
            Text="{x:Bind Nota, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap" />

        <StackPanel
            Grid.Row="3"
            Margin="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                x:Name="SaveButton"
                Click="SaveButton_Click"
                Content="Guardar"
                Style="{StaticResource AccentButtonStyle}" />
            <Button
                x:Name="CancelButton"
                Click="CancelButton_Click"
                Content="Cancelar" />
        </StackPanel>
    </Grid>
</UserControl>
