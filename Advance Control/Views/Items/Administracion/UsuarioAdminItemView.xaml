<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Advance_Control.Views.Items.Administracion.UsuarioAdminItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d">

    <Border
        Margin="0,0,0,12"
        Padding="12"
        Background="{x:Bind ResolveBackground(Usuario.EstaActiva), Mode=OneWay}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <StackPanel Spacing="6">
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontStyle="Italic" Text="Nombre:" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Medium" Text="{x:Bind FormatNombre(Usuario.Tratamiento, Usuario.Nombre, Usuario.Apellido, Usuario.ContactoId), Mode=OneWay}" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontStyle="Italic" Text="Usuario:" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Medium" Text="{x:Bind Usuario.Usuario, Mode=OneWay}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontStyle="Italic" Text="Nivel:" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Medium" Text="{x:Bind Usuario.TipoUsuario.Nivel, Mode=OneWay}" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontStyle="Italic" Text="Tipo:" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Medium" Text="{x:Bind Usuario.TipoUsuario.TipoUsuario, Mode=OneWay}" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontStyle="Italic" Text="Nota:" />
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="Medium" Text="{x:Bind Usuario.TipoUsuario.Nota, Mode=OneWay}" TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </StackPanel>
            </Grid>
            <Grid Grid.Column="1">
                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button Click="EditarButton_Click" Content="Editar" />
                    <Button Click="EliminarButton_Click" Content="Desactivar" />
                </StackPanel>
            </Grid>
        </Grid>





    </Border>
</UserControl>
