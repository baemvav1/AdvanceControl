# Visual Guide - Estado de Cuenta XML Feature

## UI Layout

```
┌─────────────────────────────────────────────────────────────┐
│  Estado de Cuenta    [Cargar Archivo XML]   (Ctrl+O)       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ⭕ Loading...  (ProgressRing - shown while loading)        │
│                                                              │
│  ℹ️ Success: Archivo cargado exitosamente. 3 transacciones │
│  ❌ Error: Error al cargar el archivo...  [x]              │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  Información General                                         │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ Banco:          Banco Ejemplo                          │ │
│  │ Sucursal:       Sucursal Centro                        │ │
│  │ Titular:        Juan Pérez López                       │ │
│  │ Número Cuenta:  1234567890                            │ │
│  │ Período:        Enero 2024                            │ │
│  │ Fecha Inicio:   2024-01-01                            │ │
│  │ Fecha Fin:      2024-01-31                            │ │
│  │ Saldo Inicial:  10000.00                              │ │
│  │ Saldo Final:    8500.50                               │ │
│  │ Total Cargos:   2500.00                               │ │
│  │ Total Abonos:   1000.50                               │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                              │
│  Transacciones                                               │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ ┌─────────────────────────────────────────────────┐   │ │
│  │ │ 2024-01-05  Compra en supermercado              │   │ │
│  │ │ Cargo                              -500.00      │   │ │
│  │ │ REF123456                            9500.00    │   │ │
│  │ └─────────────────────────────────────────────────┘   │ │
│  │ ┌─────────────────────────────────────────────────┐   │ │
│  │ │ 2024-01-10  Depósito en sucursal                │   │ │
│  │ │ Abono                              1000.50      │   │ │
│  │ │ REF789012                          10500.50     │   │ │
│  │ └─────────────────────────────────────────────────┘   │ │
│  │ ┌─────────────────────────────────────────────────┐   │ │
│  │ │ 2024-01-15  Pago de servicios                   │   │ │
│  │ │ Cargo                             -2000.00      │   │ │
│  │ │ REF345678                           8500.50     │   │ │
│  │ └─────────────────────────────────────────────────┘   │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## User Flow

```
    ┌─────────┐
    │  Start  │
    └────┬────┘
         │
         v
    ┌────────────────────┐
    │ Click "Cargar      │
    │ Archivo XML"       │
    │ (or Ctrl+O)        │
    └────┬───────────────┘
         │
         v
    ┌─────────────────────┐
    │ Windows File Picker │
    │ Opens (*.xml only)  │
    └────┬────────────────┘
         │
         v
    ┌──────────┐       ┌────────────┐
    │ Cancel?  ├──Yes─→│  No Action │
    └────┬─────┘       └────────────┘
         │No
         v
    ┌───────────────────┐
    │ Show Loading      │
    │ Indicator         │
    └────┬──────────────┘
         │
         v
    ┌───────────────────┐
    │ Read XML File     │
    │ Content           │
    └────┬──────────────┘
         │
         v
    ┌───────────────────┐
    │ Parse XML with    │
    │ ParsearEstado     │
    │ CuentaXml()       │
    └────┬──────────────┘
         │
         ├──Error─→ ┌──────────────┐
         │          │ Show Error   │
         │          │ InfoBar      │
         │          └──────────────┘
         │
         v Success
    ┌───────────────────┐
    │ Update UI with    │
    │ - Estado de Cuenta│
    │ - Transacciones   │
    └────┬──────────────┘
         │
         v
    ┌───────────────────┐
    │ Show Success      │
    │ InfoBar           │
    └────┬──────────────┘
         │
         v
    ┌─────────┐
    │   End   │
    └─────────┘
```

## Component Architecture

```
┌──────────────────────────────────────────────────────────┐
│                      EsCuentaView.xaml                    │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  Button, ProgressRing, InfoBars                     │ │
│  │  TextBlocks, ListView                                │ │
│  └────────────────┬────────────────────────────────────┘ │
└───────────────────┼───────────────────────────────────────┘
                    │ Data Binding
                    │ (x:Bind)
                    v
┌──────────────────────────────────────────────────────────┐
│              EsCuentaViewModel.cs                         │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  Properties:                                         │ │
│  │  - EstadoCuenta (EstadoCuenta?)                     │ │
│  │  - Transacciones (ObservableCollection)            │ │
│  │  - IsLoading (bool)                                 │ │
│  │  - ErrorMessage (string?)                           │ │
│  │  - SuccessMessage (string?)                         │ │
│  │                                                      │ │
│  │  Methods:                                            │ │
│  │  - CargarArchivoXmlAsync(nint windowHandle)        │ │
│  │  - ParsearEstadoCuentaXml(string xmlContent)       │ │
│  └────────────────┬────────────────────────────────────┘ │
└───────────────────┼───────────────────────────────────────┘
                    │ Uses
                    v
┌──────────────────────────────────────────────────────────┐
│                    Models                                 │
│  ┌─────────────────┐     ┌──────────────────┐           │
│  │  EstadoCuenta   │     │  Transaccion     │           │
│  ├─────────────────┤     ├──────────────────┤           │
│  │ - NumeroCuenta  │     │ - Fecha          │           │
│  │ - Titular       │     │ - Descripcion    │           │
│  │ - Banco         │     │ - Monto          │           │
│  │ - Sucursal      │     │ - Tipo           │           │
│  │ - Periodo       │     │ - Saldo          │           │
│  │ - FechaInicio   │     │ - Referencia     │           │
│  │ - FechaFin      │     └──────────────────┘           │
│  │ - Saldos...     │                                     │
│  │ - Transacciones │◄────1:N relationship               │
│  └─────────────────┘                                     │
└──────────────────────────────────────────────────────────┘
```

## Data Flow

```
XML File → FileOpenPicker → FileIO.ReadTextAsync → XML String
                                                        │
                                                        v
                                        XDocument.Parse(xmlContent)
                                                        │
                                                        v
                                        ┌───────────────────────┐
                                        │ ParsearEstadoCuenta   │
                                        │ Xml()                 │
                                        └───────┬───────────────┘
                                                │
                    ┌───────────────────────────┼───────────────────────┐
                    │                           │                       │
                    v                           v                       v
            ┌──────────────┐          ┌──────────────┐      ┌────────────────┐
            │ Extract      │          │ Parse        │      │ Parse          │
            │ General Info │          │ Decimal      │      │ Transacciones  │
            │ (strings)    │          │ Values       │      │ Collection     │
            └──────┬───────┘          └──────┬───────┘      └────────┬───────┘
                   │                         │                       │
                   └─────────────────┬───────┴───────────────────────┘
                                     │
                                     v
                          ┌──────────────────┐
                          │  EstadoCuenta    │
                          │  object          │
                          └─────────┬────────┘
                                    │
                                    v
                          ┌──────────────────┐
                          │ Update ViewModel │
                          │ Properties       │
                          └─────────┬────────┘
                                    │
                                    v
                          ┌──────────────────┐
                          │ UI Updates via   │
                          │ Data Binding     │
                          └──────────────────┘
```

## Key Features Visualization

### 1. Button with Keyboard Shortcut
```
┌─────────────────────────────┐
│  [📁] Cargar Archivo XML    │  ← Button
│       (Ctrl+O)              │  ← Keyboard Accelerator
└─────────────────────────────┘
```

### 2. Loading State
```
When IsLoading = true:
┌─────────────────────────────┐
│     ⭕ Loading...            │  ← ProgressRing Active
│  [📁] Cargar Archivo XML    │  ← Button Disabled
└─────────────────────────────┘
```

### 3. Error Handling
```
Try-Catch Block → ErrorMessage Property → InfoBar
                                            │
                                            v
                        ┌─────────────────────────────┐
                        │ ❌ Error: [Message]    [x]  │
                        └─────────────────────────────┘
```

### 4. Success Feedback
```
Successful Parse → SuccessMessage Property → InfoBar
                                               │
                                               v
                        ┌──────────────────────────────────────┐
                        │ ✓ Archivo cargado. 3 transacciones   │
                        └──────────────────────────────────────┘
```

## XML Parsing Strategy

```
XML Structure:
<EstadoCuenta>
  ├── <NumeroCuenta>       → estadoCuenta.NumeroCuenta
  ├── <Titular>            → estadoCuenta.Titular
  ├── <SaldoInicial>       → decimal.TryParse() → estadoCuenta.SaldoInicial
  └── <Transacciones>
      ├── <Transaccion>
      │   ├── <Fecha>      → transaccion.Fecha
      │   ├── <Monto>      → decimal.TryParse() → transaccion.Monto
      │   └── ...
      └── <Transaccion>
          └── ...
```

## Converters Usage

### BoolNegationConverter
```
IsLoading = true  →  BoolNegationConverter  →  false  →  Button IsEnabled
IsLoading = false →  BoolNegationConverter  →  true   →  Button IsEnabled
```

### NullToBoolConverter
```
ErrorMessage = null     →  NullToBoolConverter  →  false  →  InfoBar IsOpen
ErrorMessage = "Error"  →  NullToBoolConverter  →  true   →  InfoBar IsOpen
```

## Security Features

```
┌────────────────────────────────────────────┐
│  Security Layer                            │
├────────────────────────────────────────────┤
│  ✓ Windows FileOpenPicker (Secure)        │
│  ✓ XDocument.Parse (XXE Protected)        │
│  ✓ TryParse (No Exception on Invalid)     │
│  ✓ Culture-Invariant Parsing              │
│  ✓ Try-Catch Error Handling               │
│  ✓ No Direct File I/O                     │
│  ✓ No SQL Operations                      │
└────────────────────────────────────────────┘
```

## Testing Results

```
✓ XML file selection
✓ XML parsing with sample data
✓ General information extraction
✓ Multiple transactions parsing
✓ Decimal value conversion (culture-invariant)
✓ Optional fields handling (null-safe)
✓ Error handling and user feedback
✓ UI state management (loading, success, error)
```
