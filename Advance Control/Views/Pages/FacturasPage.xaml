<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Advance_Control.Views.Pages.FacturasPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Advance_Control.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10">
            <TextBlock
                VerticalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Facturas CFDI" />
            <Button
                x:Name="BtnCargarFacturaXml"
                Click="BtnCargarFacturaXml_Click"
                Content="Cargar XML"
                IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}" />
            <Button
                x:Name="BtnCargarMultiplesFacturas"
                Click="BtnCargarMultiplesFacturas_Click"
                Content="Subir varios XML"
                IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}" />
            <Button
                x:Name="BtnActualizarFacturas"
                Click="BtnActualizarFacturas_Click"
                Content="Actualizar listado"
                IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ProgressRing
                Grid.Column="0"
                Width="40"
                Height="40"
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />

            <StackPanel Grid.Column="1">
                <InfoBar
                    IsClosable="True"
                    IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
                    Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                    Severity="Error" />

                <InfoBar
                    Margin="0,8,0,0"
                    IsClosable="True"
                    IsOpen="{x:Bind ViewModel.SuccessMessage, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
                    Message="{x:Bind ViewModel.SuccessMessage, Mode=OneWay}"
                    Severity="Success" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Border
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="2"
                CornerRadius="8">
                <Grid RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Facturas guardadas" />
                        <TextBlock
                            Margin="0,4,0,0"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Selecciona una factura para abrir su detalle." />
                        <TextBlock
                            Margin="0,6,0,0"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind ViewModel.ResumenFacturasExistentes, Mode=OneWay}" />
                    </StackPanel>

                    <Border
                        Grid.Row="1"
                        Padding="12"
                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <StackPanel Spacing="12">
                            <TextBlock FontWeight="SemiBold" Text="Filtros" />
                            <Grid ColumnSpacing="8" RowSpacing="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBox
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    PlaceholderText="Buscar por folio"
                                    Text="{x:Bind ViewModel.FolioFiltro, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                <TextBox
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    PlaceholderText="Buscar por receptor"
                                    Text="{x:Bind ViewModel.ReceptorFiltro, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                <TextBox
                                    Grid.Row="0"
                                    Grid.Column="2"
                                    PlaceholderText="Buscar por metodo de pago"
                                    Text="{x:Bind ViewModel.MetodoPagoFiltro, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                <TextBox
                                    Grid.Row="0"
                                    Grid.Column="3"
                                    PlaceholderText="Buscar por total"
                                    Text="{x:Bind ViewModel.TotalFiltro, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                <CalendarDatePicker
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Header="Desde"
                                    Date="{x:Bind ViewModel.FechaFiltroDesde, Mode=TwoWay}" />

                                <CalendarDatePicker
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Header="Hasta"
                                    Date="{x:Bind ViewModel.FechaFiltroHasta, Mode=TwoWay}" />

                                <Button
                                    Grid.Row="1"
                                    Grid.Column="5"
                                    VerticalAlignment="Bottom"
                                    Click="BtnLimpiarFiltrosFacturas_Click"
                                    Content="Limpiar" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <ListView
                        Grid.Row="2"
                        x:Name="FacturasListView"
                        Padding="8"
                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        IsItemClickEnabled="True"
                        ItemClick="FacturasListView_ItemClick"
                        ItemsSource="{x:Bind ViewModel.FacturasExistentes, Mode=OneWay}"
                        SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:FacturaResumenDto">
                                <Border
                                    Margin="0,0,0,10"
                                    Padding="12"
                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6">
                                    <StackPanel Spacing="6">
                                        <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding FolioTitulo}" />
                                        <TextBlock
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="{Binding EmisorReceptorTexto}"
                                            TextWrapping="WrapWholeWords" />
                                        <TextBlock
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="{Binding FechaTexto}" />
                                        <TextBlock
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="{Binding TotalesTexto}"
                                            TextWrapping="WrapWholeWords" />
                                        <TextBlock FontWeight="SemiBold" Text="{Binding UuidTexto}" TextWrapping="WrapWholeWords" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
