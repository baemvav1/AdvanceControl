<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Advance_Control.Views.Equipos.SeleccionarServicioUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    MinWidth="500"
    MinHeight="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search filters -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12" Spacing="8">
            <TextBox
                x:Name="ConceptoTextBox"
                PlaceholderText="Buscar por concepto..."
                TextChanged="SearchTextBox_TextChanged" />
            <TextBox
                x:Name="DescripcionTextBox"
                PlaceholderText="Buscar por descripción..."
                TextChanged="SearchTextBox_TextChanged" />
        </StackPanel>

        <!-- Loading indicator -->
        <ProgressRing
            x:Name="LoadingRing"
            Grid.Row="1"
            Width="50"
            Height="50"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsActive="False" />

        <!-- Lista de servicios -->
        <ListView
            x:Name="ServiciosListView"
            Grid.Row="1"
            SelectionMode="Single"
            SelectionChanged="ServiciosListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- ID Badge -->
                        <Border
                            Grid.Row="0"
                            Grid.RowSpan="3"
                            Grid.Column="0"
                            Margin="0,0,12,0"
                            Padding="8,4"
                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                            CornerRadius="4"
                            VerticalAlignment="Center">
                            <TextBlock
                                Text="{Binding IdServicio}"
                                Foreground="White"
                                FontWeight="SemiBold"
                                FontSize="12" />
                        </Border>

                        <!-- Concepto -->
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="1"
                            Text="{Binding Concepto}"
                            FontWeight="SemiBold"
                            FontSize="14"
                            TextTrimming="CharacterEllipsis" />

                        <!-- Descripción -->
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="1"
                            Text="{Binding Descripcion}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            FontSize="12"
                            TextTrimming="CharacterEllipsis" />

                        <!-- Costo -->
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="1"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            FontSize="12">
                            <Run Text="Costo: $" />
                            <Run Text="{Binding Costo}" />
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Proveedores button and grid -->
        <StackPanel x:Name="ProveedoresPanel" Grid.Row="2" Margin="0,12,0,0" Visibility="Collapsed">
            <Button x:Name="ProveedoresButton"
                    Content="Proveedores"
                    Click="ProveedoresButton_Click"
                    HorizontalAlignment="Stretch"
                    Margin="0,0,0,8" />
            <Border x:Name="ProveedoresGrid"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12"
                    MinHeight="100"
                    Visibility="Collapsed">
                <TextBlock Text="Lista de proveedores" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
